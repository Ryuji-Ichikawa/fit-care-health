default: &default
  adapter: mysql2
  encoding: utf8
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  username: root
  password:
  socket: /tmp/mysql.sock

development:
  <<: *default
  database: fit_care_health_development

test:
  <<: *default
  database: fit_care_health_test

production:
  <<: *default
  database: fit_care_health_production
  username: fit_care_health
  password: password